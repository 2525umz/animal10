</div>
<br><br>
<div class="row">
  <div class="col-lg-2">
  </div>
<h3>いいね一覧</h3>
</div>

<div class="row">
  <div class="col-lg-2">
  </div>
<% @favorites.each do |favorite|%>
 <%= link_to item_path(favorite.item.id) do %>
  <%= attachment_image_tag favorite.item, :image, size: "50x50", fallback: "no_image.jpg"%>
 <% end %>
  <%= favorite.item.name %>
<% end%>
</div>